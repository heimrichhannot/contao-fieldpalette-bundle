<a href="{{ href }}"
    {{ cssClass ? 'class="' ~ cssClass ~ '" '|raw }}
    {{ title ? 'title="' ~ title ~ '" '|raw }}
    {{ accesskey ? 'accesskey="' ~ accesskey ~ '" '|raw }}
    {{ attributes }}
>
    {{ label }}
</a>